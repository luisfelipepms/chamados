spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/chamados
    username: chamados
    password: chamados

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true